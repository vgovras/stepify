import 'package:flutter/material.dart';

/// Design system color tokens — dark theme only.
///
/// Ported from `tasks/demo.html` CSS `:root` variables.
abstract final class AppColors {
  // Backgrounds
  static const bg = Color(0xFF0E0D0B);
  static const s1 = Color(0xFF1A1814);
  static const s2 = Color(0xFF242118);
  static const br = Color(0xFF2E2B24);

  // Accents
  static const ac = Color(0xFFE8B44C);
  static const ac2 = Color(0xFFD4793A);

  // Text
  static const tx = Color(0xFFF0EAD8);
  static const t2 = Color(0xFF8A8070);
  static const t3 = Color(0xFF5A5448);

  // Status
  static const gr = Color(0xFF6AAF6A);
  static const rd = Color(0xFFD46060);
}
