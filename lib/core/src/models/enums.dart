/// Recipe difficulty level.
enum Difficulty { easy, medium, hard }

/// Recipe category for filtering.
enum RecipeCategory { soup, main, breakfast, salad }

/// Ingredient grouping for shopping list display.
enum IngredientCategory { meat, vegs, dairy, eggs, other }

/// Step note severity for visual styling.
enum NoteType { warn, tip, pro }
